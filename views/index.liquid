{% render 'partials/header.liquid', title: 'mijn progressie' %}
    <main>
        <section class="main-score">
            <p>whs</p>
            <h1>{{ golfer.handicap | round: 1}}</h1>
            <p>{{ golfer.improvement | round: 1}}</p>
            <p> 
                was {{ golfer.previous_handicap | round: 1}} op
                {{ golfer.previous_date | date: "%d %b %y" }}
             &#183; low {{ golfer.low | round: 1}}
            </p>
        </section>
        <section>
            <p>verloop</p>
            <h2>
                Hoe sta je ervoor?
            </h2>
            <img src="assests/grafiek.png" width="200" height="100" alt="grafiek">
        </section>
        <section>
            <p>log</p>
            <h2>
                Recente rondes
            </h2>
            <article>
                <h3>
                    Recente rondes
                </h3>
                <p>5 rondes</p>
                <ul>
                {% for score in scores %}
                <li>
                    <p class="round-date">{{ score.date | date: "%d %b %y" }}</p>
                    <p class="round-course">{{ score.course }}</p>
                    <p class="round-type">{{ score.type }}</p>
                    <p class="round-score">{{ score.differential | round: 1 }}</p>
                    <form method="post" action="/score-verwijderen">
                        <input type="hidden" name="score_id" value="{{ score.id }}">
                        <button>delete</button>
                    </form>
                </li>
                {% endfor %}
                </ul>
            </article>
        </section>
        <form method="POST" action="/score-toevoegen">
            <input type="hidden" name="golfer_id" value="{{ golfer.id }}">
            <label>
                Datum:
                <input type="date" name="date" placeholder="1 jan 2026">
            </label>
            <label>
                Baan:
                <input type="text" name="course">
            </label>
            <label>
                Score
                <input type="number" name="score">
            </label>
            <label>
                Telt mee voor handicap
                <input type="checkbox" checked name="type" value="QUAL">
            </label>
            <button>versturen</button>
        </form>
        {% comment %} <section>
            <p>volgende handicap section</p>
        </section>
        <section>
            <p>ranking section</p>
            {% for ranking in rankings %}
                {{ranking.category}}
                {{ ranking.rankings }}
            {% endfor %}
        </section>
        <section>
            <p>mijlpalen section</p>
            {% for milestone in milestones %}
                {{milestone.title}}
            {% endfor %}
        </section> {% endcomment %}
    </main>
</body>
</html>